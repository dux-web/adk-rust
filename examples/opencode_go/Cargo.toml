[package]
name = "opencode-go-example"
version = "0.0.1"
edition = "2024"
publish = false

[workspace]

[dependencies]
adk-core = { path = "../../adk-core" }
adk-model = { path = "../../adk-model", default-features = false, features = ["opencode-go"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
futures = "0.3"
